<Window x:Class="HCIProject02.GUI.Dialog.DialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HCIProject02.GUI.Dialog"
        mc:Ignorable="d"        
        ResizeMode="{Binding ResizeMode}"
        Title="{Binding Title}" Height="{Binding WindowHeight, Mode=TwoWay}" Width="{Binding WindowWidth, Mode=TwoWay}">
    <Grid>
        <ContentControl x:Name="ContentPresenter" Content="{Binding}"/>
    </Grid>
</Window>
